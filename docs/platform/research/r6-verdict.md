# R6 — corrections, verification and decision

**Research cycle complete: proceed with the bounded local saved-study implementation.** The modest EC2 hosting direction remains conditional on the stated public-library and cloud gates. No further catalogue-research cycle is needed to choose the first local slice; implementation is the next source of evidence.

The independent [fresh review](r5-fresh-eyes.md#r6-correction-check) verified all six corrections and raised its assessment from **8/10 to 9/10 for the bounded implementation decision**. That is not a production-readiness score. The central [proposal](platform-investigation.md) carries the final conditions beside the recommendations.

## Corrections closed

| Finding                                                                 | Applied correction                                                                                                                                                                                 |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Saved geometry disconnected from storage cost                           | Added fixture-byte sensitivities and separate imported/saved/revision quantities; a trial 8.662 GB paired-payload allocation sits inside explicit root/headroom/retention bounds.                  |
| Recovery drawings conflated scheduled backups and revocation durability | Separate journal, content backups, key recovery and independent task registry/reaper; named logical principals and cutoff proofs; physical stores and costs are explicit hosted prerequisites.     |
| First deliverable and local retention unclear                           | Local view-save first, separate editable recovery, named desktop trial support, device-local loss semantics, export/import and visible rebuild state; engineering totals and exclusions collected. |
| CloudFront DNS/cancellation metering caveat missing                     | Added DNS pay-as-you-go fallback and subscription cancellation/attachment drift to reconciliation.                                                                                                 |
| Exported diagrams lost conditions                                       | Qualified direct-host model and managed alternative; browser containment, conditional credential retention and funded retry after old-task reconciliation; regenerated SVGs.                       |
| Diagram quality lacked observable tests                                 | Three reader tasks with a counterbalanced pilot, stated correctness/uncertainty criteria and a failure response; no quality improvement claimed yet.                                               |

The final retry contract also records ECS's finite client-token deduplication window. An unresolved launch is reconciled rather than replayed blindly after that window may have expired. This correction is grounded in the [primary ECS contract](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ECS_Idempotency.html), not an assumed permanent exactly-once guarantee.

## Constructed evidence and final checks

Root reran the snapshot experiment on supported **Node 24.19.0**, redirecting its output to a separate retained record and adding the runtime value. All six SVG hashes, focus comparisons, fixture digest and sizes match the original. This run measured **25.02 ms decode/reindex**, **4.645 s construction** and **0.444 s layout**. The [record](evidence/r6-snapshot.json) preserves the run; the original 16 ms single-run evidence was not overwritten. Neither is browser paint or application-reopen latency. The reviewer independently checked consistency of this record after its own earlier execution on an unsupported older Node version.

All five DOT diagrams regenerate without Graphviz warnings. Root visually inspected the four changed SVGs after regeneration. The unchanged delivery diagram was covered by the fresh review. The central Mermaid source contains three diagrams and was checked against its surrounding contracts; no dedicated Mermaid browser-render test is claimed.

The [final validation manifest](evidence/final-validation.json) records the mechanical checks and working-file hashes. The 37 application/package/engine-preparation inputs remain unchanged from the R1 source manifest, preserving the existing Tal/application work. Documentation hashes refer to this working tree, not the merged application commit.

Gitleaks reported two generic-key matches in the initial input manifest. Both are reviewed SHA-256 **file fingerprints**, at the identity-document and DiagramKey file entries; they are not credentials. The final scan uses only those exact finding fingerprints as temporary exceptions. Evidence was retained and no broad rule was disabled.

The personal-brain synthesis is stored at `/Users/jeanluciradukunda/myspace/personal-brain/docs/enpassant-platform-research.md`, linked from its existing Enpassant project and dossier index. It preserves concrete experiments and points to application-owned evidence rather than copying datasets. The temporary usage-limit rejection of that save was resolved on the authorized retry. No content was published.

## Residual gates and final call

Browser persistence, bounded decoding, editable state, owner isolation, job races and lifetime counts need the specified implementation tests. Region/size, CloudFront enrollment, effective IAM, real task/reaper timing, backup/journal/key recovery and complete costs need a selected account and separately authorized cloud experiment. Reader benefit requires the declared user tasks. Domain/provider choices and operating commitments remain explicit decisions.

These are named evidence dependencies, not unsupported claims silently accepted by the investigation. The unchanged repository strategy does not rely on the unavailable GNU FAQ full-page check; a concrete proprietary/distribution change would reopen that focused source/legal question. No paid resources, backend feature, visibility change, commit or PR was created by this research cycle.

**Final call: close research and begin the small local build when implementation is requested.** Reconcile that build against the saved-study acceptance matrix before expanding to the public platform. A green local slice must not be used to waive the separate hosted gates.
